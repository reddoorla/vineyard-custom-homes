name: ci
on:
  pull_request:
  push:
    branches: [main, staging]
jobs:
  ci:
    permissions:
      contents: read
      pull-requests: write
    uses: reddoorla/.github/.github/workflows/ci.yml@c714d9e472885bbf66f386e9f056a16aab7986d2 # v1.4.2
    with:
      node-version: "24"
      netlify-site: "vineyard-custom-homes-staging"
