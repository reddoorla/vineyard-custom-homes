name: ci
on:
  pull_request:
  push:
    branches: [main, staging]
jobs:
  ci:
    permissions:
      contents: read
      pull-requests: write
    uses: reddoorla/.github/.github/workflows/ci.yml@558395431ddcb481ecba3dd84b78b38c338cfa03 # v1.4.0
    with:
      node-version: "24"
      netlify-site: "vineyard-custom-homes-staging"
